# Substack-sync security verification

Generated by Track F (Apps Scripts templating), 2026-05-03.

## What this confirms

1. The unrotated RBN password that lives inside `backfillRBN()` in the live Apps Script editor is **NOT** present in this repository in any form. The local snapshot `Code.gs` (gitignored) marks it `<redacted>`; the sanitized template `Code.template.gs` omits the entire `backfillRBN()` function and its helpers.
2. The placeholder `__RBN_PASSWORD_FROM_KEYCHAIN__` is the only credential token left in the template, used solely as documentation for any future per-tenant reimplementation. It must never be replaced with a literal in any committed file.
3. `.gitignore` blocks `google-scripts/substack-sync*/Code.gs` (and clasp byproducts) while explicitly allowing the sanitized template + docs.
4. The Substack live source (`Code.gs`) is verified gitignored at the time of this note and is not in tracked-changes.
5. **Hard rule going forward:** Substack live source must NEVER be committed. Any future PR introducing `google-scripts/substack-sync/Code.gs` (or a sibling `*-clasped/` source dump) requires the leaked-credential rotation in `README.md § Secret rotation` to be complete first AND the password literal to be excised from the editor.

## Verification — `git status --porcelain google-scripts/`

### Before producing the templates

```
?? google-scripts/
```

(Whole `google-scripts/` directory was untracked — no individual files visible at top level. `grep -i substack` against this output returned no matches; exit code 1.)

### After producing the templates

```
?? google-scripts/
```

### Detailed file-level view (after)

`git ls-files --others --exclude-standard google-scripts/`:

```
google-scripts/CLASP_BLOCKED.md
google-scripts/MULTI_TENANT_PORTING.md
google-scripts/STATUS.md
google-scripts/beside-action-router/Code.gs
google-scripts/beside-action-router/Code.template.gs
google-scripts/beside-action-router/README.md
google-scripts/otter-drive-watcher/Code.gs
google-scripts/otter-drive-watcher/Code.template.gs
google-scripts/otter-drive-watcher/README.md
google-scripts/substack-sync/Code.template.gs
google-scripts/substack-sync/README.md
```

`git ls-files --others --ignored --exclude-standard google-scripts/`:

```
google-scripts/substack-sync/Code.gs
```

### Substack-grep verification

`git status --porcelain google-scripts/ | grep -i substack` → exit 1 (no match). The Substack live source is invisible to porcelain status because the parent `google-scripts/` directory aggregates untracked entries; `Code.gs` itself appears only in the explicit `--ignored` listing, confirming it is gitignored.

## .gitignore rules in effect (as of this note)

```
google-scripts/substack-sync/
google-scripts/substack-sync*/
google-scripts/substack-sync*/Code.gs
google-scripts/substack-sync*/Code.js
google-scripts/substack-sync*/appsscript.json
google-scripts/substack-sync*/.clasp.json
google-scripts/substack-sync-clasped/
google-scripts/substack-sync-clasped*/
```

Per-file `git check-ignore -v` results:
- `google-scripts/substack-sync/Code.gs` → ignored by line `google-scripts/substack-sync*/Code.gs`
- `google-scripts/substack-sync/Code.template.gs` → NOT ignored (exit 1)
- `google-scripts/substack-sync/README.md` → NOT ignored (exit 1)
