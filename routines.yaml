# routines.yaml — single source of truth for SKILLs + daemons
# Generated by overnight Phase 1.7 (2026-05-02)
#
# Schema (per PLAN v3.1 A-fast.8):
#   name           — current name (pre-rename); rename target in `rename_to`
#   kind           — skill | daemon | server
#   wrapper_label  — LaunchAgent label that runs it (null = no wrapper, runs ad-hoc)
#   schedule       — cron-like description (verbatim from plist or SKILL)
#   depends_on     — list of routine names this depends on
#   package        — capture | briefing | research | deals | server | infra
#   mode           — subscription | api | daemon (per PLAN §6 decisions)
#   max_daily_usd  — soft cap (per Track C6); null until cost data exists
#   confidentiality — public | tenant | private
#   rename_to      — proposed new name (null = no rename)
#   notes
#
# IMPORTANT — this file documents current state, not desired state.
# Renames are NOT applied. LaunchAgents are NOT modified. Per HARD RULES.

version: 1
generated_at: "2026-05-02T23:55:00-04:00"
source_truth_for: ["skills", "daemons", "dashboard_server"]

# ───────────────────────────────────────────────────────────────────
# Server
# ───────────────────────────────────────────────────────────────────

server:
  - name: cosdashboard
    kind: server
    wrapper_label: com.yoni.cosdashboard
    program: ~/cos-pipeline/cos-dashboard-server.py
    schedule: keepalive (RunAtLoad)
    package: server
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.dashboard
    notes: "Port 7777 (tomac); plan §J1 reserves 7778+ for additional tenants."

# ───────────────────────────────────────────────────────────────────
# SKILLs (canonical at ~/.claude/scheduled-tasks/ OR
#         ~/dashboards/scheduled-tasks/ — pending C-2 resolution).
# Total: 24 in ~/.claude / 23 in ~/dashboards.
# ───────────────────────────────────────────────────────────────────

skills:

  # — Capture pipeline —
  - name: beside-pipeline
    kind: skill
    wrapper_label: null
    schedule: "(no LaunchAgent; manual or other trigger)"
    package: capture
    mode: subscription
    confidentiality: tenant
    rename_to: capture-phone-notes
    notes: "Only present in ~/.claude/, not ~/dashboards/. Asymmetric. Pulls Beside.com phone notes."

  - name: cos-capture-pipeline
    kind: skill
    wrapper_label: com.yoni.claude-task.cos-capture-pipeline
    schedule: "(check plist)"
    package: capture
    mode: subscription
    confidentiality: tenant
    rename_to: capture-inbox

  - name: cos-email-resolver-morning
    kind: skill
    wrapper_label: null
    schedule: "(daemon email-resolver runs 07:30/18:00 instead — see DAEMONS.md DUP)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: capture-email-resolver
    notes: "DUP with com.tomaccove.email-resolver — daemon is the live path."

  - name: cos-email-resolver-evening
    kind: skill
    wrapper_label: null
    schedule: "(daemon email-resolver runs 07:30/18:00 instead)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: "(merge into capture-email-resolver)"
    notes: "DUP — merge per PLAN rename map line 86."

  - name: cos-gmail-mini
    kind: skill
    wrapper_label: null
    schedule: "(daemon gmail-mini runs M-F every 2h instead — see DAEMONS.md DUP)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: capture-email-triage
    notes: "Likely DUP with com.tomaccove.gmail-mini."

  - name: cos-otter-transcripts
    kind: skill
    wrapper_label: null
    schedule: "(daemon otter-backfill runs every 15 min instead — possible DUP)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: capture-call-transcripts

  - name: cos-otter-transcripts-afternoon
    kind: skill
    wrapper_label: null
    schedule: "(merge per PLAN rename map)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: "(merge into capture-call-transcripts)"

  - name: cos-otter-transcripts-midday
    kind: skill
    wrapper_label: null
    schedule: "(merge per PLAN rename map)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: "(merge into capture-call-transcripts)"

  - name: cos-otter-transcripts-backfill-now
    kind: skill
    wrapper_label: null
    schedule: "(merge with --backfill flag)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: "(merge into capture-call-transcripts)"

  # — Briefing pipeline —
  - name: cos-personal-briefing
    kind: skill
    wrapper_label: com.yoni.claude-task.cos-personal-briefing
    schedule: "(daily, see plist)"
    package: briefing
    mode: subscription
    confidentiality: tenant
    rename_to: briefing-morning

  - name: notebooklm-daily-briefing
    kind: skill
    wrapper_label: com.yoni.claude-task.notebooklm-daily-briefing
    schedule: "(daily, Tue–Fri)"
    package: briefing
    mode: subscription
    confidentiality: tenant
    rename_to: "(replaced by briefing-morning if Track G validates)"

  - name: notebooklm-sunday-weekly-briefing
    kind: skill
    wrapper_label: com.yoni.claude-task.notebooklm-sunday-weekly-briefing
    schedule: "Sunday"
    package: briefing
    mode: subscription
    confidentiality: tenant
    rename_to: "(merge into briefing-weekly)"

  - name: sunday-weekly-email
    kind: skill
    wrapper_label: com.yoni.claude-task.sunday-weekly-email
    schedule: "Sunday"
    package: briefing
    mode: subscription
    confidentiality: tenant
    rename_to: briefing-weekly

  - name: master-daily-update
    kind: skill
    wrapper_label: null
    schedule: "(no wrapper — verify if active)"
    package: briefing
    mode: subscription
    confidentiality: tenant
    rename_to: briefing-market-update

  # — Deals pipeline —
  - name: tomac-cove-weekly-pipeline
    kind: skill
    wrapper_label: com.yoni.claude-task.tomac-cove-weekly-pipeline
    schedule: "weekly"
    package: deals
    mode: subscription
    confidentiality: tenant
    rename_to: deals-weekly-scan

  - name: tomac-deal-compile
    kind: skill
    wrapper_label: com.yoni.claude-task.tomac-deal-compile
    schedule: "(see plist)"
    package: deals
    mode: subscription
    confidentiality: tenant
    rename_to: deals-compile

  # — Research pipeline (Track H consolidates these into one parameterized SKILL) —
  - name: gs-research-daily-download
    kind: skill
    wrapper_label: com.yoni.claude-task.gs-research-daily-download
    schedule: "daily"
    package: research
    mode: subscription
    confidentiality: tenant
    rename_to: research-fetch-gs
    notes: "Interim name — Track H builds a single research-fetch SKILL parameterized by --vendor."

  - name: gs-research-pdf-processor
    kind: skill
    wrapper_label: com.yoni.claude-task.gs-research-pdf-processor
    schedule: "daily (after fetch)"
    package: research
    mode: subscription
    confidentiality: tenant
    rename_to: research-process-gs

  - name: jefferies-pdf-downloader
    kind: skill
    wrapper_label: com.yoni.claude-task.jefferies-pdf-downloader
    schedule: "weekly"
    package: research
    mode: subscription
    confidentiality: tenant
    rename_to: research-fetch-jefferies

  - name: jefferies-pdf-processor
    kind: skill
    wrapper_label: com.yoni.claude-task.jefferies-pdf-processor
    schedule: "weekly (after fetch)"
    package: research
    mode: subscription
    confidentiality: tenant
    rename_to: research-process-jefferies

  - name: rbn-daily-sync
    kind: skill
    wrapper_label: com.yoni.claude-task.rbn-daily-sync
    schedule: "daily"
    package: research
    mode: subscription
    confidentiality: tenant
    rename_to: research-fetch-rbn

  - name: peakload-weekly-sync
    kind: skill
    wrapper_label: com.yoni.claude-task.peakload-weekly-sync
    schedule: "weekly"
    package: research
    mode: subscription
    confidentiality: tenant
    rename_to: research-fetch-peakload

  - name: run-syncall-gas
    kind: skill
    wrapper_label: com.yoni.claude-task.run-syncall-gas
    schedule: "(orchestrator)"
    package: research
    mode: subscription
    confidentiality: tenant
    rename_to: research-fetch-all

  # — Podcasts —
  - name: podcast-transcribe-daily
    kind: skill
    wrapper_label: com.yoni.claude-task.podcast-transcribe-daily
    schedule: "daily"
    package: capture
    mode: api
    confidentiality: public
    rename_to: podcasts-transcribe
    notes: "Uses AssemblyAI (paid API) for transcription, not subscription."

# ───────────────────────────────────────────────────────────────────
# Daemons (com.tomaccove.* — 17 total)
# Schedules verbatim from PlistBuddy. See DAEMONS.md for full table.
# ───────────────────────────────────────────────────────────────────

daemons:

  - name: scheduler
    kind: daemon
    wrapper_label: com.tomaccove.scheduler
    program: ~/tomac-cove-pipeline/call_scheduler.py
    schedule: keepalive
    package: infra
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.scheduler

  - name: phone-watcher
    kind: daemon
    wrapper_label: com.tomaccove.phone-watcher
    program: ~/tomac-cove-pipeline/phone_call_watcher.py
    schedule: keepalive
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.phone-watcher

  - name: recorder.menu
    kind: daemon
    wrapper_label: com.tomaccove.recorder.menu
    program: ~/tomac-cove-pipeline/call_recorder_menu.py
    schedule: keepalive
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.recorder.menu

  - name: recorder.start.gcal.*
    kind: daemon
    wrapper_label: "com.tomaccove.recorder.start.gcal.* (2 instances)"
    program: ~/tomac-cove-pipeline/otter_record.sh start
    schedule: one-shot calendar-driven (regenerated by call_scheduler.py)
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: "(call_scheduler.py must emit com.cos.tomac.* labels)"
    notes: "4 plists total (2 start + 2 stop). Already-fired schedules; harmless residue."

  - name: recorder.stop.gcal.*
    kind: daemon
    wrapper_label: "com.tomaccove.recorder.stop.gcal.* (2 instances)"
    program: ~/tomac-cove-pipeline/otter_record.sh stop
    schedule: one-shot calendar-driven
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: "(see above)"

  - name: email-resolver
    kind: daemon
    wrapper_label: com.tomaccove.email-resolver
    program: ~/dashboards/scripts/email-resolver-runner.sh -> ~/dashboards/routines/process/cos_email_resolver.py
    schedule: "daily 07:30 + 18:00"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.capture-email-resolver
    notes: "DUP with cos-email-resolver-{morning,evening} SKILLs — daemon is canonical, runner script header confirms."

  - name: email-backfill
    kind: daemon
    wrapper_label: com.tomaccove.email-backfill
    program: ~/dashboards/scripts/email-backfill-runner.sh
    schedule: "every 900s (15 min)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.email-backfill

  - name: gmail-mini
    kind: daemon
    wrapper_label: com.tomaccove.gmail-mini
    program: ~/dashboards/scripts/gmail-mini-runner.sh
    schedule: "M–F 08:05–20:05 every 2h (35 entries)"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.capture-email-triage
    notes: "Likely DUP with cos-gmail-mini SKILL."

  - name: otter-backfill
    kind: daemon
    wrapper_label: com.tomaccove.otter-backfill
    program: ~/dashboards/scripts/otter-backfill-runner.sh
    schedule: "every 900s"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.otter-backfill

  - name: dash-corrections
    kind: daemon
    wrapper_label: com.tomaccove.dash-corrections
    program: ~/dashboards/scripts/dash-corrections-runner.sh
    schedule: "daily 22:00"
    package: capture
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.dash-corrections

  - name: calendar.renew
    kind: daemon
    wrapper_label: com.tomaccove.calendar.renew
    program: ~/tomac-cove-pipeline/call_scheduler.py register
    schedule: "every 172800s (48h)"
    package: infra
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.calendar-renew

  - name: chrome-mcp
    kind: daemon
    wrapper_label: com.tomaccove.chrome-mcp
    program: ~/dashboards/scripts/run-chrome-mcp-daemon.sh
    schedule: keepalive
    package: infra
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.chrome-mcp

  - name: cloudflared
    kind: daemon
    wrapper_label: com.tomaccove.cloudflared
    program: cloudflared tunnel run call-scheduler
    schedule: keepalive
    package: infra
    mode: daemon
    confidentiality: tenant
    rename_to: com.cos.tomac.cloudflared

  - name: ngrok
    kind: daemon
    wrapper_label: com.tomaccove.ngrok
    program: ngrok http 8765
    schedule: keepalive
    package: infra
    mode: daemon
    confidentiality: tenant
    rename_to: "(verify still in use; if not, retire)"
    notes: "Possible legacy — cloudflared may have superseded."

  - name: usage-report
    kind: daemon
    wrapper_label: com.tomaccove.usage-report
    program: ~/dashboards/scripts/anthropic-usage-report-runner.py
    schedule: "Monday 07:00"
    package: infra
    mode: api
    confidentiality: tenant
    rename_to: com.cos.tomac.usage-report

# ───────────────────────────────────────────────────────────────────
# Coverage summary
# ───────────────────────────────────────────────────────────────────
coverage:
  skills_total:    24   # ~/.claude tree (23 in ~/dashboards)
  skills_wrapped:  15   # have a com.yoni.claude-task.* plist
  skills_unwrapped: 9   # likely deprecated in favor of daemons (or merge targets)
  daemons_total:   17
  duplicates_high_confidence:
    - "email-resolver daemon ↔ cos-email-resolver-{morning,evening} SKILLs"
    - "gmail-mini daemon ↔ cos-gmail-mini SKILL"
  duplicates_to_verify:
    - "otter-backfill daemon ↔ cos-otter-transcripts* SKILLs"
